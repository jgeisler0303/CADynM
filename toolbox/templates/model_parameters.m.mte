parameter_names= {
{{
    % temp_data is expected to be a MultiBodySystem
    pn= temp_data.params.getParamNames();
    for i = 1:temp_data.params.getNumParams()
        fprintf("    '%s'\n", pn{i});
    end
}}};
