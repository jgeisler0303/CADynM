{{ fprintf('%% Multibody system: "%s", generated using backend %s', temp_data.Name, temp_data.getsetSymbolicBackend); }}

nq = {{ fprintf("%d", temp_data.getNumDOF+temp_data.getNumAux); }};
nx = {{ fprintf("%d", 2*temp_data.getNumDOF+2*temp_data.getNumAux); }};
nu = {{ fprintf("%d", temp_data.getNumIn); }};
ny = {{ fprintf("%d", temp_data.getNumOut); }};

{{ 
for i = 1:temp_data.getNumDOF
    fprintf("%s_idx= %d;\n", temp_data.getQName(i), i);
end
for i = 1:temp_data.getNumAux
    fprintf("%s_idx= %d;\n", temp_data.getAuxName(i), i+temp_data.getNumDOF);
end
}}
{{ 
for i = 1:temp_data.getNumDOF
    fprintf("%s_idx= %d;\n", temp_data.getQdName(i), i+temp_data.getNumDOF+temp_data.getNumAux);
end
for i = 1:temp_data.getNumAux
    fprintf("%s_idx= %d;\n", temp_data.getAuxName(i, 1), i+2*temp_data.getNumDOF+temp_data.getNumAux);
end
}}
{{ 
for i = 1:temp_data.getNumIn
    fprintf("in_%s_idx= %d;\n", temp_data.getInName(i), i);
end
}}
{{ 
for i = 1:temp_data.getNumOut
    fprintf("out_%s_idx= %d;\n", temp_data.getOutName(i), i);
end
}}
dof_names= {
{{ 
for i = 1:temp_data.getNumDOF
    fprintf("    '%s'\n", temp_data.getQName(i));
end
for i = 1:temp_data.getNumAux
    fprintf("    '%s'\n", temp_data.getAuxName(i));
end
}}};

dof_d_names= {
{{ 
for i = 1:temp_data.getNumDOF
    fprintf("    '%s'\n", temp_data.getQdName(i));
end
for i = 1:temp_data.getNumAux
    fprintf("    '%s'\n", temp_data.getAuxName(i, 1));
end
}}};

state_names= {
{{ 
for i = 1:temp_data.getNumDOF
    fprintf("    '%s'\n", temp_data.getQName(i));
end
for i = 1:temp_data.getNumAux
    fprintf("    '%s'\n", temp_data.getAuxName(i));
end
for i = 1:temp_data.getNumDOF
    fprintf("    '%s'\n", temp_data.getQdName(i));
end
for i = 1:temp_data.getNumAux
    fprintf("    '%s'\n", temp_data.getAuxName(i, 1));
end
}}};

input_names= {
{{ 
for i = 1:temp_data.getNumIn
    fprintf("    '%s'\n", temp_data.getInName(i));
end
}}};

output_names= {
{{ 
for i = 1:temp_data.getNumOut
    fprintf("    '%s'\n", temp_data.getOutName(i));
end
}}};
